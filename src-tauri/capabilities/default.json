{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for local application windows.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
